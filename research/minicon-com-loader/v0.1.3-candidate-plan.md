# v0.1.3 Candidate plan — `minicon.com` as fourth Release asset

Status: **plan only**. Do not `git tag`, do not GitHub Release, do not
overwrite `v0.1.2`.

Ruling (cdx, 2026-08-29): option **A**. Frozen `v0.1.2` three packages stay
the stable baseline. `v0.1.3` Candidate = those three packages **plus**
independent `minicon.com` + independent SHA-256 sidecar + build/aggregate
receipts. Promotion copies **exact Candidate bytes**; it must not rebuild.

Research smoke at `459d5cb` run `33238478661` is **not** this Candidate.
It only proves one-pack six-cell `--version` / argv / `--status`.

## Sequence (do not skip)

1. Land this plan (this file + PRD pointer).
2. Implement gates G1 scaffolding, G3 installer lock, G4 size table
   (still on Cargo `0.1.2` tree is allowed for harness only).
3. Implement G2 GUI+control black-box on six native cells (no compile on
   the matrix).
4. Single clean-tree version bump to `0.1.3` (G7) then **one** pack (G1).
5. G5 download-rehash-execute on that exact blob; G6 Defender+360 on that
   SHA; G8 human confirm then tag.

Any red cell after the `0.1.3` pack → discard Candidate, do not tag.

## Frozen / bans

| ban | check |
|-----|--------|
| no mix into `v0.1.2` | `gh release view v0.1.2 --json assets` still exactly the original three archives + sidecars |
| no rebuild on promote | Candidate `minicon_com_sha256` == Release asset sha256 |
| no dirty pack | `source_dirty=false` in build receipt |
| no compile on test matrix | test jobs: no `cargo`, no `cosmocc`, no repo checkout except downloaded artifact |
| no fake GUI | a cell without an interactive desktop must use a real equivalent runner (UTM/Lima/`xvfb`+Wayland as already used by product black-box), never “compile instead” |

## G1 — exact clean source, one pack, bound receipt

**Do**

```bash
git status --porcelain   # must be empty
git rev-parse HEAD       # record SOURCE
python3 research/minicon-com-loader/write-build-receipt.py   # after pack
```

GitHub: `workflow_dispatch` of `.github/workflows/minicon-com.yml` on that
`SOURCE` only (after G7 bump). One macos-15 pack, never a second pack for
the same Candidate.

**Receipt must contain**

- `source_sha` == `SOURCE`
- `source_dirty` == false
- `source_tree_digest`
- `product_version` == `0.1.3`
- `minicon_com_sha256` / `minicon_com_bytes`
- per-cell payload sha256 + bytes (already in `payloads`)
- `tools`: rustc, zig, cargo-zigbuild, cargo-xwin, cosmocc identity
- loader identity: sha256 of `research/minicon-com-loader/loader.c` (add if missing)

**Pass:** pack job green; JSON has every field; `source_dirty=false`.
**Fail:** dirty tree, second pack, missing field.

## G2 — six native cells: smoke + GUI/control black-box

Smoke (already in `minicon-com.yml`): `--version` equals `minicon 0.1.3`,
unknown flag nonzero, `--status` has `pty backend`.

**Add** (new step, same downloaded `minicon.com`, still no compile):

Minimum control journey on each cell:

1. start GUI (`minicon.com` with isolated `MINICON_CONFIG` / socket)
2. `wait-ready` (or documented equivalent rendezvous)
3. `list-tabs`
4. `send-text` to the active tab
5. `capture-pane` **or** `ui-snapshot`
6. clean exit (`close-window` / process exit 0); no leftover extract dir

Cells and runners (same labels as smoke):

| cell | runner | GUI court |
|------|--------|-----------|
| osx-aarch64 | macos-15 | native Aqua |
| osx-x86_64 | macos-15-intel | native Aqua |
| win-x86_64 | windows-2025 | native GUI; `Start-Process` + control pipe |
| win-aarch64 | windows-11-arm | native GUI |
| lnx-x86_64 | ubuntu-24.04 | `xvfb` / existing product Wayland-or-X black-box, **not** compile |
| lnx-aarch64 | ubuntu-24.04-arm | same |

Local lane may use Lima/UTM as the Linux/Windows courts; GitHub native
runners are the Candidate-of-record courts.

**Pass:** six runtime receipts + aggregate; each receipt `job_status=success`
and a black-box log digest; same `minicon_com_sha256` / `source_tree_digest`.
**Fail:** skip GUI, compile-on-matrix, or drop a cell.

## G3 — loader / installer regressions

Must stay green on the Candidate pack host (macos-15 and this Mac):

```bash
bash research/minicon-com-loader/reaper-tests.sh
bash research/minicon-com-loader/install-cosmocc-swap-test.sh
# plus (to add): concurrent two-process start of minicon.com
# plus (to add): SIGKILL child / waitpid -8 skip unlink
# plus (to add): installer flock/single-instance; swap-test asserts rc=2
#                and nested sentinel / tree manifest
```

CI pack is already one job → one installer. Still add a lock file next to
`DEST` so two overlapping `install-cosmocc.sh` cannot delete each other's
PREV. Proof of CI single-instance is not enough if local pack can race.

**Pass:** all listed scripts exit 0; no extract dir left for dead pids that
we own; mid-swap restore keeps old bin digest.
**Fail:** symlink follow, missing lock, swap-test rc≠2 on injected fail.

## G4 — size court (budget after one measured table)

`minicon.com` is a zip-of-six-GUIs, **not** bound by the historical sub-1-MiB
single-PE ceiling. Do not reuse `release_budget_bytes = 1,048,575` for the
APE.

**Do** (script to add, run after pack):

```bash
python3 research/minicon-com-loader/write-size-report.py
```

Report must list: raw `minicon.com` bytes, `gzip -9`/`zip` recompress bytes,
six payload bytes, cosmocc trampoline remainder.

**Proposed freeze (replace with measured Candidate numbers, then stop):**

Until the first clean `0.1.3` pack, fail closed if raw `minicon.com` >
`12582912` (12 MiB) — headroom over the ~8.5 MiB smoke blob. After the
Candidate pack, overwrite this row in PRD with exact bytes + a reviewed
ceiling (Candidate + 5% or +256 KiB, whichever larger). Over ceiling →
Candidate fail.

**Pass:** report committed next to receipt; bytes ≤ ceiling.
**Fail:** missing breakdown or over ceiling.

## G5 — Release fourth asset + re-hash + execute

Extend `.github/workflows/release.yml` (or a dedicated promote workflow)
so a **dry_run** Candidate:

- keeps building the original three packages from the **same** `SOURCE`
- **does not rebuild** `minicon.com`; it downloads the exact G1 artifact
- uploads `minicon.com`, `minicon.com.sha256`, `build-receipt.json`,
  `aggregate-receipt.json`
- a later job downloads each of the four assets, hashes, and executes
  `minicon.com --version` / `--status` on at least one native Windows
  runner and one POSIX runner

**Pass:** four assets + sidecars; downloaded sha256 matches receipt;
execute matches `minicon 0.1.3`.
**Fail:** pack `minicon.com` again on promote; missing sidecar.

Live `gh release create` stays **off** until G8.

## G6 — reputation on exact Candidate SHA

On the Candidate `minicon_com_sha256` only (not a rebuild):

- Windows Defender scan evidence (native Windows court)
- 360 scan evidence (operator-supplied snapshot; SHA visible)

**Pass:** both clean at that SHA; evidence files named by SHA under
`research/minicon-com-loader/dist/` are **gitignored**; hashes recorded in
the Candidate receipt.
**Fail:** any hit → no Promotion; open vendor false-positive, keep PTY/
control features.

## G7 — version identity

One commit, clean tree:

- `Cargo.toml` / lock `version = "0.1.3"`
- six payloads `--version` → `minicon 0.1.3`
- `minicon.com --version` → `minicon 0.1.3` (payload, not cosmocc GCC)
- loader/receipt `product_version` `0.1.3`

**Pass:** all four spellings match.
**Fail:** mixed 0.1.2/0.1.3.

## G8 — no tag if any cell red; human Promotion

- Candidate workflow all green (G1–G5 automated parts, G3, G4, G7)
- G6 evidence attached
- human (chairman or cdx) replies **promote** on the mux thread
- then `git tag v0.1.3` on `SOURCE` and Release of **exact** Candidate
  bytes
- no GitHub pre-release / draft as a substitute for v0.1.3

This agent does not tag without that human line.

## File owners (when implementing)

| area | files |
|------|--------|
| plan (this) | `research/minicon-com-loader/v0.1.3-candidate-plan.md` |
| smoke + GUI matrix | `.github/workflows/minicon-com.yml`, `ci-status.sh`, `ci-status.ps1`, new `ci-control.*` |
| receipts | `write-build-receipt.py`, size report script |
| loader/installer | `loader.c`, `reaper-tests.sh`, `install-cosmocc.sh`, swap-test |
| three+one publish | `.github/workflows/release.yml` |
| PRD | `prd/PRD_02_27_con_delivery.md`, `PRD.md` Release evolution node |

## Current vs Candidate

| item | now | Candidate |
|------|-----|-----------|
| Cargo | 0.1.2 | 0.1.3 |
| smoke six-cell | green `33238478661` | must re-run on 0.1.3 bytes |
| GUI+control six-cell | absent | G2 |
| installer lock | absent | G3 |
| size budget for APE | unset | G4 |
| Release fourth asset | no | G5 after G8 |
| tag | none | only after G8 |
