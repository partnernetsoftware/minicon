# v0.1.3 Candidate plan — six-cell archives + `minicon.com`

Status: **implementation active; G8 forbidden**. Do not `git tag`, do not GitHub Release, do not
overwrite `v0.1.2`.

The frozen `v0.1.2` Release stays untouched: its three archives cover only
4/6 cells because macOS Universal carries both slices while Windows arm64 and
Linux arm64 are absent. `v0.1.3` must publish five archives — Windows/Linux
x86_64+arm64 and macOS Universal — plus independent `minicon.com`; all six
assets have SHA-256 sidecars. Promotion copies **exact Candidate bytes** and
must not rebuild.

Research smoke at `459d5cb` run `33238478661` is **not** this Candidate.
It only proves one-pack six-cell `--version` / argv / `--status`.

Horizon qjswasm+TinyVM (PRD `[ ]` not v0.1.3) is **out of this plan**. Do not
fold it into G1–G8, the 9 MiB ceiling, bump, or tag.

## Sequence (do not skip)

1. Revise this plan (cdx 四点 + 两点) — this commit.
2. Implement **G3** (installer lock, swap-test rc=2 + manifest, SIGKILL vs
   waitpid-fail tests).
3. **G4 rehearsal:** pack a non-Candidate blob, emit size table, fail-closed
   at 12 MiB only as a rehearsal guard. Chairman/cdx **stamp a hard ceiling
   into source + PRD**. That commit becomes part of later SOURCE.
4. Implement **G2** only after Windows isolation **choice B** evidence
   (below). Still Cargo `0.1.2` until G7. G3 and G4 rehearsal are **not**
   blocked.
5. **G7** one clean bump to `0.1.3`, then **one** unsigned pack (G1) against
   the **already committed** ceiling. The company-signing court consumes that
   pack exactly once, records before→after SHA, and six native G2 courts execute
   the signed after-SHA. Candidate sealing consumes only this signed court.
   Candidate must not write/overwrite budget rows.
6. G5 re-hash/execute of the sealed six-asset set; G6 Defender into **qualification/
   aggregate** receipt only; G8 only if 董事长/用户 replies exact version +
   promote.

Any red cell after the Candidate pack → discard Candidate, do not tag.

## Frozen / bans

| ban | check |
|-----|--------|
| no mix into `v0.1.2` | `gh release view v0.1.2 --json assets` still exactly the original three archives + sidecars |
| no rebuild on promote | all six Candidate assets (5 archives + `minicon.com`) downloaded and published unchanged |
| no dirty pack | `source_dirty=false` in build receipt |
| no compile on test matrix | test jobs: no `cargo`, no `cosmocc`, no repo checkout except downloaded artifact |
| no fake GUI | a cell without an interactive desktop must use a real equivalent runner (UTM/Lima/`xvfb`+Wayland as already used by product black-box), never “compile instead” |

## G1 — exact clean source, one pack, bound receipt

**Do**

```bash
git status --porcelain   # must be empty
git rev-parse HEAD       # record SOURCE
python3 research/minicon-com-loader/write-build-receipt.py   # after pack
```

GitHub: `workflow_dispatch` of `.github/workflows/minicon-com.yml` on that
`SOURCE` only (after G4 stamp + G7 bump). One macos-15 pack, never a second
pack for the same Candidate. `.github/workflows/company-signing.yml` then
consumes that successful run, transforms only `minicon.com` and the two Windows
payloads, and runs the signed APE in six native GUI/control courts. G4 hard
ceiling is already in this SOURCE; neither workflow may edit budget files.

**Receipt must contain**

- `source_sha` == `SOURCE`
- `source_dirty` == false
- `source_tree_digest`
- `product_version` == `0.1.3`
- `minicon_com_sha256` / `minicon_com_bytes`
- per-cell payload sha256 + bytes (already in `payloads`)
- `tools`: rustc, zig, cargo-zigbuild, cargo-xwin, cosmocc identity
- loader identity: sha256 of `research/minicon-com-loader/loader.c` (add if missing)

**Pass:** pack job green; JSON has every field; `source_dirty=false`; signing
receipt binds the unsigned build SHA to the signed after-SHA without changing
the build receipt.
**Fail:** dirty tree, second pack, missing field.

## G2 — six native cells: smoke + GUI/control black-box

Smoke (already in `minicon-com.yml`): `--version` equals `minicon 0.1.3`,
unknown flag nonzero, `--status` has `pty backend`.

**Add** (new step, same downloaded `minicon.com`, still no compile):

Product code has **no** `wait-ready` verb. Do not borrow UTM court
`wait-ready` (that is VM job-agent protocol, not MiniCon).

There is **no** `MINICON_CONFIG` environment variable in this repository.
Do not invent one.

Config path truth (agenterm-platform `649174e3` `runtime::user_config_directory`):

- Darwin / Linux: `HOME` + `/.config/minicon.json`. Linux does **not** read
  `XDG_CONFIG_HOME` for MiniCon config. `XDG_CONFIG_HOME` / `XDG_RUNTIME_DIR`
  may be set as **desktop/runtime auxiliaries** (sockets, portals) but are
  not the product config root.
- Windows: `SHGetFolderPathW(CSIDL_APPDATA, SHGFP_TYPE_CURRENT)` +
  `minicon.json`. **Not** `USERPROFILE` / `APPDATA` env. Setting those
  variables does **not** isolate Windows config.

**Windows G2 isolation = choice B** (choice A, a tested cross-platform
config-path override in MiniCon, is **not** taken for this Candidate).

B means:

- Candidate-of-record Windows cells run on a **clean dedicated** court:
  GitHub-hosted `windows-2025` / `windows-11-arm` ephemeral VM, or local
  UTM **disposable** clone. Reused interactive profiles are not G2 evidence.
- Before GUI start, record a **read-only baseline** of the CSIDL_APPDATA
  `minicon.json` path (absent or hashed bytes). After exit, the same path
  is in the black-box log. Baseline is evidence, not an env override.
- Still required on every cell (including Windows): unique `--control`
  (`unix:<abs>/control.sock` or `pipe:<unique-name>`) and a unique working
  directory for this run's files.

Darwin/Linux may isolate `HOME` to a temp tree (that **does** move
`~/.config/minicon.json`).

Ready rendezvous: loop `minicon.com cli --control ENDPOINT list-tabs` until
it succeeds (or a new script that is only that loop). Then:

1. start GUI (`minicon.com --control ENDPOINT` under the isolated env)
2. ready = `list-tabs` loop
3. `send-text` to a tab from `list-tabs`
4. `capture-pane` **or** `ui-snapshot`
5. clean exit (`close-window` and/or process exit 0); no leftover extract dir

Cells and runners (same labels as smoke):

| cell | runner | GUI court |
|------|--------|-----------|
| osx-aarch64 | macos-15 | native Aqua |
| osx-x86_64 | macos-15-intel | native Aqua |
| win-x86_64 | windows-2025 | native GUI; `Start-Process` + control pipe |
| win-aarch64 | windows-11-arm | native GUI |
| lnx-x86_64 | ubuntu-24.04 | `xvfb` / existing product Wayland-or-X black-box, **not** compile |
| lnx-aarch64 | ubuntu-24.04-arm | same |

Local lane may use Lima/UTM as the Linux/Windows courts; GitHub native
runners are the Candidate-of-record courts.

**Pass:** six runtime receipts + aggregate; each receipt `job_status=success`
and a black-box log digest; same `minicon_com_sha256` / `source_tree_digest`;
Windows receipts include CSIDL_APPDATA `minicon.json` **baseline before start
and uncontaminated (unchanged vs baseline) after clean exit**.
**Fail:** skip GUI; compile-on-matrix; drop a cell; claim Windows isolation
via `USERPROFILE`/`APPDATA`; use `XDG_CONFIG_HOME` as MiniCon config root;
omit baseline or post-run contamination check.

Rehearsal evidence (not Candidate): run `33244331387` at `5776086` passed all
six native GUI/control cells and the same-digest/source/run aggregate. Windows
evidence includes the real CSIDL config hash before/after and zero persistent
extract directories. The bytes still identify as `0.1.2`; G2 must run again
after the single clean G7 identity bump.

## G3 — loader / installer regressions

Must stay green on the Candidate pack host (macos-15 and this Mac):

```bash
bash research/minicon-com-loader/reaper-tests.sh
bash research/minicon-com-loader/install-cosmocc-swap-test.sh
```

Add, as **separate** tests (do not merge SIGKILL with `-8`):

| test | child fate | waitpid | extract dir | process exit |
|------|------------|---------|-------------|--------------|
| T-sigkill | payload SIGKILL'd | success, `WIFSIGNALED` | **cleaned** | `128+SIGKILL` (nonzero) |
| T-waitpid-fail | inject non-EINTR `waitpid` error | failure (`-8`) | **kept** (child state unknown) | nonzero, no unlink |

Also: concurrent two-process `minicon.com` start (two extract dirs, both
private); installer flock/single-instance next to `DEST`; swap-test asserts
`rc=2`, old bin digest, nested sentinel, and a directory manifest of PREV.

CI pack being one job is not a lock. Two local `install-cosmocc.sh` must not
delete each other's PREV.

**Pass:** reaper 4-class + T-sigkill + T-waitpid-fail + swap-test all green.
**Fail:** SIGKILL leaves the dir, or waitpid-fail unlinks, or swap-test rc≠2.

## G4 — size court (rehearsal measure → stamped ceiling → Candidate)

`minicon.com` is a zip-of-six-GUIs, **not** bound by the historical sub-1-MiB
single-PE ceiling. Do not reuse `release_budget_bytes = 1,048,575` for the
APE.

Candidate **must not set its own budget**. A clean Candidate pack cannot
afterward overwrite a PRD row: that commit would not be the Candidate
`SOURCE`.

**Phase R (rehearsal, not Candidate)**

```bash
# pack on current tree; this blob is not v0.1.3 Candidate
python3 research/minicon-com-loader/write-size-report.py
```

Report: raw `minicon.com` bytes, gzip-9 of the container, six *unpacked*
payload file bytes, and a Zip-central-directory breakdown (`ape_prefix_bytes`,
`cells_compressed_bytes`, `cells_uncompressed_bytes`, CD/EOCD). Do **not**
call `payload_sum - com_bytes` a trampoline remainder. Rehearsal
**fail-closed guard only**: raw >
`12582912` (12 MiB) → stop and investigate. 12 MiB is **not** the
Candidate ceiling.

**Stamp (done 2026-08-29, cdx):** `CANDIDATE_CEILING_BYTES = 9437184` (9 MiB)
in `write-size-report.py` and `prd/PRD_02_27_con_delivery.md`. Tests: ceiling
pass, ceiling+1 fail. 12 MiB remains rehearsal guard only. A later unique
pack over 9 MiB fails and re-opens budget review; never auto-raise.

**Phase C (unique Candidate pack on a later clean SHA)**

Pack once. Compare against the **already committed** ceiling. Do not edit
PRD/budget files in the Candidate SOURCE after pack.

**Pass:** stamped ceiling exists in tree; Candidate bytes ≤ ceiling.
**Fail:** missing stamp, Candidate writes budget, or over ceiling.

## G5 — full six-cell Release set + re-hash + execute

**Candidate construction** consumes the six native payloads from the **same
one-pack run** as G1. It must not invoke Cargo/cosmocc again. Package Windows
and Linux x86_64+arm64 independently and merge the two already-built macOS
slices into one Universal archive. Seal five archives + `minicon.com`, their
six sidecars, and build/aggregate receipts.

**Promotion** must **download and lift those six original Candidate
assets**. It must not rebuild `minicon.com` or any archive. Verify: SHA-256 of
each promoted asset equals the sealed
Candidate receipt.

A native package court downloads, re-hashes, unpacks and executes all six
covered cells: Windows x86_64+arm64, Linux x86_64+arm64 and both slices of the
macOS Universal archive. A later Promotion court re-downloads the six promoted
assets and executes `minicon.com --version` / `--status` on native Windows and
POSIX runners.

**Pass:** five archives + `minicon.com`, six sidecars, all six native cells
execute `minicon 0.1.3`, and every SHA-256 matches the sealed Candidate.
**Fail:** any rebuild on promote; missing sidecar.

Live `gh release create` stays **off** until G8.

## G6 — reputation on exact Candidate SHA

On the signed Candidate `minicon_com_sha256` only (not a rebuild):

- Windows Defender scan evidence (native Windows court)
- company Authenticode identity and RFC 3161 timestamp on Windows artifacts;
  `minicon.com` must reserve its certificate directory at APE link time

The exact order is build → sign → signed six-grid → Candidate seal → Defender.
The dedicated `release-signing` GitHub Environment uses Azure Artifact Signing
Public Trust through OIDC. Its three OIDC identifiers are Environment secrets;
endpoint/account/profile are Environment variables. The managed signing key is
not exportable and never enters GitHub. See `prd/PRD_02_27_con_delivery.md` for
the redacted custody/backup contract.

**Pass:** the signed final SHA is clean in Defender. Evidence files (gitignored) and their
hashes go into a **post-pack qualification / aggregate receipt**. They
must **not** mutate the sealed G1 `build-receipt.json`.
**Fail:** any hit → no Promotion; open vendor false-positive, keep PTY/
control features. Editing the sealed build receipt is also fail.

Raw Defender evidence remains operator-held and gitignored. The locally
verified redacted qualification receipt is admitted by
`.github/workflows/reputation.yml`, which binds it to the successful Candidate
run/source/APE digest and publishes a short-lived Actions artifact. Promotion
requires that exact successful reputation run ID and revalidates the receipt;
an absent, red, mismatched or expired qualification blocks Release.

## G7 — version identity

One commit, clean tree:

- `Cargo.toml` / lock `version = "0.1.3"`
- six payloads `--version` → `minicon 0.1.3`
- `minicon.com --version` → `minicon 0.1.3` (payload, not cosmocc GCC)
- loader/receipt `product_version` `0.1.3`

**Pass:** all four spellings match.
**Fail:** mixed 0.1.2/0.1.3.

## G8 — no tag if any cell red; Promotion authority

cdx is **not** a release authority. cdx may review gates and report. This
agent does not tag on a cdx “promote”.

Promotion requires an explicit reply from **董事长/用户** that names the
**exact version** and the word **promote** (example: `v0.1.3 promote`).
Anything else (cdx ack, “looks good”, mux thumbs-up) is not Promotion.

- Candidate workflow all green (G1–G5 automated parts, G3, stamped G4, G7)
- G6 qualification/aggregate evidence attached, build receipt untouched
- then `git tag v0.1.3` on `SOURCE` and Release of the **exact** six-asset
  Candidate set
- no GitHub pre-release / draft as a substitute for v0.1.3

## File owners (when implementing)

| area | files |
|------|--------|
| plan (this) | `research/minicon-com-loader/v0.1.3-candidate-plan.md` |
| smoke + GUI matrix | `.github/workflows/minicon-com.yml`, `ci-status.sh`, `ci-status.ps1`, new `ci-control.*` |
| receipts | `write-build-receipt.py`, size report script |
| loader/installer | `loader.c`, `reaper-tests.sh`, `install-cosmocc.sh`, swap-test |
| six-cell package + APE publish | `.github/workflows/release.yml` |
| post-pack reputation admission | `.github/workflows/reputation.yml`, `reputation_court.py` |
| PRD | `prd/PRD_02_27_con_delivery.md`, `PRD.md` Release evolution node |

## Current vs Candidate

| item | now | Candidate |
|------|-----|-----------|
| Cargo | 0.1.3 | 0.1.3 |
| smoke six-cell | one-pack run `33250748539` green | passed |
| GUI+control six-cell | one-pack run `33250748539` green | passed |
| installer lock | pack-host G3 receipt green and sealed | passed |
| size budget for APE | 9 MiB stamped; exact APE 8,909,564 bytes | passed |
| full six-cell Release set + APE | unsigned rehearsal Candidate `33250998985` sealed; six native executions green | rerun after company-signing court; old unsigned Candidate is not promotable |
| signing boundary | `company-signing.yml` + exact-byte receipt/verifier implemented; not yet dispatched | Azure Public Trust identity/OIDC configured; three files validly signed and six after-SHA courts green |
| reputation | unsigned exact APE detected by Defender as `Program:Win32/Contebrew.A!ml`; link-time Authenticode padding proven locally | scan the signed Candidate after-SHA; no Promotion while red |
| tag | none | only after G8 |

Latest unsigned rehearsal (not Candidate): `33259779184` at `033e582` passed
G1/G2/G3 input-side checks; SHA `b2551896e7985c80eea4ea0ac785d532e9efb477aafd475955afbe9f735b0c2a`,
8,909,562 bytes, signable PE header and six-cell aggregate. Pack time was
4m38s. It remains unsigned and must not be fed directly to Candidate sealing.
Its local `self-sign-rehearsal.sh` mechanism court also passed: Security
Directory 0→1,712 bytes, final APE 8,911,280 bytes, ZIP and Darwin execution
green, and signed-byte tamper rejected. The ephemeral untrusted certificate
had no timestamp and was destroyed; this evidence is explicitly not G6.
